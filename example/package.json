{
  "name": "example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pretest": "npm uninstall textlint-rule-no-mix-dearu-desumasu && npm install",
    "test": "textlint --rule no-mix-dearu-desumasu README.md -f pretty-error;test $? -eq 1"
  },
  "author": "azu",
  "license": "MIT",
  "dependencies": {
    "textlint": "^6.0.0",
    "textlint-rule-no-mix-dearu-desumasu": "file:.."
  }
}
